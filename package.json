{
  "name": "island-royale-multiplayer-v47-ready-mode-lock",
  "version": "1.0.0",
  "description": "Host-ready multiplayer battle royale with server-enforced mode counts and ready checks.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  },
  "engines": {
    "node": ">=18"
  }
}
